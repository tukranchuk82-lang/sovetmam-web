// Портрет автора проекта (вырезанная фигура с прозрачным фоном).
// Обычный <img> (без оптимизатора Next) — так замена файла подхватывается сразу,
// без кэша оптимизатора. ?v=N — версия для сброса кэша браузера при замене фото.
//
// butskaya-trim.png — исходник с обрезанными пустыми полями (было 862×1032,
// фигура занимала только 691×1031: слева 80px, справа 91px прозрачности).
// Благодаря обрезке ширина <img> == ширине самой фигуры, и её можно ровно
// выровнять по ширине блока подписи под фото.
// Отдаётся в 640px: на экране портрет не шире 194 CSS-px, т.е. 582px даже при
// DPR 3 — больше грузить незачем (файл 172KB вместо 498KB у исходника).
export function Portrait({ className }: { className?: string }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/founder/butskaya-trim.png?v=1"
      alt="Татьяна Буцкая"
      width={640}
      height={955}
      className={className}
    />
  );
}
