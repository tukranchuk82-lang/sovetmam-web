// ВРЕМЕННО (с 23.09.2026, по просьбе Тани — см. память
// project_temp_reverts_sovetmam_web). На время приложение нигде не
// показывает «Совет матерей» и представляется от лица АНО «Выбор родителей».
//
// Один флаг решает всё разом — когда скажут вернуть, поменять на false,
// и все места, которые берут название отсюда, снова покажут «Совет матерей».
// Плюс public/manifest.json (имя PWA-приложения) — его руками, JSON
// констант не читает.
const TEMP_BRAND = true;

/** Название в кавычках для середины предложения («…рекомендует», «ответ …»). */
export const ORG_NAME = TEMP_BRAND ? "«Выбор родителей»" : "«Совет матерей»";

/** То же в родительном падеже. Название в кавычках по-русски обычно не
 * склоняется («проект АНО «Выбор родителей»»), но для «Совета матерей» так
 * заведено исторически (см. компонент OrgName) — оставляем ту же форму. */
export const ORG_NAME_GENITIVE = TEMP_BRAND ? "«Выбор родителей»" : "«Совета матерей»";

/** Организационно-правовая форма перед названием, без кавычек (именительный падеж, с большой буквы). */
export const ORG_LEGAL_FORM = TEMP_BRAND ? "АНО" : "Общероссийская общественная организация";

/** То же в родительном падеже, строчными — для фраз вида «председатель …». */
export const ORG_LEGAL_FORM_GENITIVE = TEMP_BRAND ? "АНО" : "общероссийской общественной организации";

/** Имя без кавычек — для JSON-LD/OpenGraph и подобных технических полей. */
export const ORG_NAME_PLAIN = TEMP_BRAND ? "Выбор родителей" : "Совет матерей";

/** Полное официальное название — для футеров писем и мета-описаний. */
export const ORG_FULL_NAME = TEMP_BRAND
  ? "АНО «Экспертно-просветительский центр поддержки материнства и детства „Выбор родителей“»"
  : "Общероссийская общественная организация «Совет матерей»";

/** Компактная форма для подписи «проект …» в местах с жёстким лимитом по
 * ширине (например, подвал PDF-подборки в одну строку без переносов) —
 * полное официальное название туда физически не влезает. */
export const ORG_FOOTER_SHORT = TEMP_BRAND ? "АНО «Выбор родителей»" : "«Совета матерей»";
