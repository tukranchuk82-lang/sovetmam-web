// Регион проживания пользователя нужен, чтобы показывать релевантные
// региональные меры. Храним выбор в cookie (читается и на сервере, и на
// клиенте). Источник истины при первом рендере: cookie → профиль → спросить.

export const REGION_COOKIE = "sm_region";

/** Срок жизни cookie с регионом — 180 дней. */
export const REGION_COOKIE_MAX_AGE = 60 * 60 * 24 * 180;
