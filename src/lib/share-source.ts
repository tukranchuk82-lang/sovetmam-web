/**
 * Метка кнопки «Поделиться» в ссылке.
 *
 * Кладём её в тот же utm_source, которым размечены рассылки и посты: приложение
 * уже умеет запоминать эту метку при первом заходе и переносить в профиль при
 * регистрации (см. utm-capture.tsx). Благодаря этому видна вся цепочка —
 * поделились, пришли, зарегистрировались, — а не только два первых шага.
 *
 * Живёт в отдельном файле, потому что нужна обеим сторонам: кнопке в браузере
 * и учёту на сервере. Соседний share.ts помечен «только сервер» и клиентскому
 * коду недоступен.
 */
export const SHARE_SOURCE = "share";
