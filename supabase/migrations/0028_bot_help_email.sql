-- Email, который человек ввёл на сайте перед тем, как уйти в бота за помощью.
--
-- Ссылки «код не пришёл» теперь несут email в start-параметре бота
-- (закодирован base64url), и вебхук /api/salebot/help его расшифровывает.
-- Без этого поля Таня видела заявку в боте, но не знала, на какую почту
-- заводить кабинет.
alter table bot_help_requests
  add column if not exists email text;
