-- «Кому положено» — условия меры человеческим текстом.
--
-- Условия отбора у нас записаны метками (criteria) — их читает движок подбора,
-- но человеку они не видны. В карточке про условия говорилось вскользь, внутри
-- краткого описания, и после чтения оставался вопрос «а мне-то положено?».
-- Теперь это отдельный блок, который идёт первым — до порядка оформления.
alter table public.measures add column if not exists eligibility text;
