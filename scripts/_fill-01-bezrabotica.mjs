// Ядро, мера 1 из 57: «Пособие по безработице».
//
// Источники, по которым выверено 23.08.2026:
//   — Федеральный закон от 12.12.2023 № 565-ФЗ «О занятости населения в РФ»
//     (ст. 23 — порядок признания безработным, ст. 24 — основания отказа);
//   — постановление Правительства РФ от 23.01.2026 № 30 — размеры пособия
//     с 1 февраля 2026 года.
//
// Цифры перепроверены дважды: популярные статьи дают устаревшие суммы
// (1 764 / 15 044 / 5 880) — это прошлогодние значения до индексации.
//
// Запуск: node scripts/_fill-01-bezrabotica.mjs           (сухой прогон)
//         node scripts/_fill-01-bezrabotica.mjs --apply    (запись)
import { createClient } from "@supabase/supabase-js";
import { readFileSync, writeFileSync } from "node:fs";

const env = {};
for (const l of readFileSync(".env.local", "utf8").split("\n")) {
  const s = l.trim(); if (!s || s.startsWith("#") || !s.includes("=")) continue;
  const i = s.indexOf("="); env[s.slice(0, i)] = s.slice(i + 1);
}
const sb = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY, {
  auth: { persistSession: false },
});
const APPLY = process.argv.includes("--apply");
const SLUG = "posobie-po-bezrabotice";

const PATCH = {
  amount: "1 863 – 15 886 ₽ в месяц (2026; в первые три месяца, далее до 6 209 ₽)",

  eligibility: [
    "Пособие назначают тем, кто не работает, готов приступить к работе и встал на учёт в службе занятости по месту жительства.",
    "Беременные и мамы с детьми до трёх лет тоже могут встать на учёт. Но пособие по безработице не платят одновременно с выплатами по беременности и родам или по уходу за ребёнком — нужно выбрать что-то одно.",
    "Размер зависит от того, сколько вы работали за последний год. Если за 12 месяцев до обращения набралось не меньше 26 недель работы, первые три месяца платят 75 % среднего заработка, следующие три — 60 %, но не выше максимальной величины. Если стажа меньше, вы ищете работу впервые или перерыв больше года — назначат минимальную величину и только на три месяца.",
    "Безработным не признают тех, кого закон считает занятыми: кто работает по договору, учится очно, оформлен как ИП или самозанятый, получает пенсию по старости. Самозанятая может встать на учёт, если сначала снимется с учёта плательщика налога на профессиональный доход.",
  ].join("\n"),

  how_to_apply: [
    "Войдите на портал «Работа России» (trudvsem.ru) через подтверждённую учётную запись Госуслуг.",
    "Составьте резюме — без него заявление не примут.",
    "Подайте заявление о содействии в поиске подходящей работы и выберите центр занятости по месту жительства.",
    "В течение одного рабочего дня вас зарегистрируют и начнут подбирать вакансии. Нужно пройти профилирование и приходить на встречи, которые назначит центр занятости.",
    "Если подходящую работу не найдут в течение десяти дней, вас признают безработным. Статус и пособие назначат задним числом — с первого дня подачи заявления.",
    "Дальше проходите перерегистрацию в назначенные дни. Два отказа от подходящей работы лишают пособия.",
  ],

  documents: [
    "Паспорт",
    "Сведения о трудовой деятельности — электронная трудовая книжка с Госуслуг или бумажная трудовая",
    "Документ об образовании и квалификации",
    "Справка о среднем заработке за три месяца перед увольнением — если работали (обычно служба занятости запрашивает её сама)",
    "Реквизиты счёта в банке для перечисления пособия",
    "Для людей с инвалидностью — индивидуальная программа реабилитации",
  ],

  tips: [
    "Размеры на 2026 год (постановление Правительства РФ от 23.01.2026 № 30): минимум 1 863 ₽, максимум 15 886 ₽ в первые три месяца и 6 209 ₽ в следующие три. Там, где действует районный коэффициент, суммы выше — например, в Алтайском крае это 2 329 ₽ и 19 858 ₽.",
    "Пособие по безработице и выплаты по уходу за ребёнком или по беременности и родам одновременно не назначают — выбирают что-то одно.",
    "Всего пособие платят не больше шести месяцев в течение года. Гражданам предпенсионного возраста — до 24 месяцев.",
    "Очная учёба, статус ИП или самозанятого закрывают право на пособие, пока вы их не прекратите.",
    "Служба занятости даёт не только деньги: бесплатное переобучение (в том числе мамам в декрете), помощь в поиске работы и социальный контракт на открытие своего дела.",
  ],

  source_name: "Федеральный закон № 565-ФЗ «О занятости населения в РФ»; постановление Правительства РФ от 23.01.2026 № 30",
  source_url: "https://www.consultant.ru/document/cons_doc_LAW_464093/",
  updated_at_label: "август 2026",
};

const { data: before, error } = await sb
  .from("measures")
  .select("slug,title,amount,eligibility,how_to_apply,documents,tips,source_name,source_url,updated_at_label,criteria")
  .eq("slug", SLUG)
  .single();
if (error) throw error;

console.log(`МЕРА: ${before.title}\n`);
console.log("БЫЛО:");
console.log(`  сумма: ${before.amount}`);
console.log(`  кому положено: ${before.eligibility ? "есть" : "— (поля не было)"}`);
console.log(`  как оформить: ${(before.how_to_apply ?? []).length} шаг(ов)`);
console.log(`  документы: ${(before.documents ?? []).length}`);
console.log(`  важно знать: ${(before.tips ?? []).length}`);
console.log(`  источник: ${before.source_name} — ${before.source_url}`);

console.log("\nСТАНЕТ:");
console.log(`  сумма: ${PATCH.amount}`);
console.log(`  кому положено: ${PATCH.eligibility.split("\n").length} абзаца`);
console.log(`  как оформить: ${PATCH.how_to_apply.length} шагов`);
console.log(`  документы: ${PATCH.documents.length}`);
console.log(`  важно знать: ${PATCH.tips.length}`);
console.log(`  источник: закон о занятости и постановление о размерах`);
console.log(`  условия отбора не трогаем: ${JSON.stringify(before.criteria)}`);

if (!APPLY) {
  console.log("\nСухой прогон. Для записи: node scripts/_fill-01-bezrabotica.mjs --apply");
  process.exit(0);
}

writeFileSync("scripts/_backup-fill-01.json", JSON.stringify(before, null, 1), "utf8");
const { error: upErr } = await sb.from("measures").update(PATCH).eq("slug", SLUG);
if (upErr) throw upErr;
console.log("\nЗаписано. Прежние значения сохранены в scripts/_backup-fill-01.json");
